version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: TAKE_CANADA_BACK-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-TAKE_CANADA_BACK}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_in_production}
      POSTGRES_DB: ${POSTGRES_DB:-TAKE_CANADA_BACK}
    volumes:
      - ./backend/data/postgres:/var/lib/postgresql/data
    # Not exposed to the host; only the backend service connects internally.
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: TAKE_CANADA_BACK-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - TAKE_CANADA_BACK_DATABASE_URL=postgresql://${POSTGRES_USER:-TAKE_CANADA_BACK}:${POSTGRES_PASSWORD:-change_me_in_production}@db:5432/${POSTGRES_DB:-TAKE_CANADA_BACK}
      - TAKE_CANADA_BACK_STORAGE_DIR=/app/data/store
      - TAKE_CANADA_BACK_TIMESTAMP_DIR=/app/data/timestamps
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: TAKE_CANADA_BACK-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
